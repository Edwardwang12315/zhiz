针对“以代码为主、人手不足”的团队，直接用 **Git 仓库完成版本存档** 是最务实的第一步。下面给你一套**零额外人力、依托开发流程自动运转**的规范操作指南。

---

## 一、三条核心规则（全员共识）

1. **一切源码、配置、文档都在仓库里**  
   不只是代码，接口说明、架构决策、部署脚本，凡是需要版本管理的文件都放入 Git。
2. **每次提交说明做了什么**  
   Commit message 按约定格式写，让 `git log` 本身就变成变更记录。
3. **用 Tag 标记每个发布版本**  
   线上运行的是哪个版本的代码，通过 Tag 秒级追溯。

---

## 二、Commit Message 规范（无需背诵，统一模板即可）

在项目根目录放一个 `commit_template.txt`，内容如下：
```
[类型] (影响范围) 简短描述

详细说明（可选，改了什么、为什么这样改）

关联 issue: #编号
```
经常使用的**类型**前缀：
| 前缀       | 含义               |
| ---------- | ------------------ |
| `feat`     | 新功能             |
| `fix`      | 修复缺陷           |
| `docs`     | 文档变更           |
| `refactor` | 重构（不改变功能） |
| `chore`    | 构建、依赖等杂项   |
| `test`     | 测试相关           |

**设置方法（每人一次）：**
```bash
git config commit.template ./commit_template.txt
```
之后每次 `git commit` 就会弹出该模板，降低思考成本。

---

## 三、版本存档的核心操作：Tag 与 Release

### 1. 打标签（Tag）代表一个发布版本

当代码通过测试、准备上线，打上版本号 Tag：
```bash
# 创建附注标签（推荐，含时间、打标签人）
git tag -a v1.2.0 -m "正式版v1.2.0：新增用户导出，修复登录超时"

# 推送标签到远程仓库（单独推送，不要用git push --tags一把梭）
git push origin v1.2.0
```

**版本号规则：** `主版本.次版本.修订号` （如 v2.1.3）
- 主版本：不兼容的大改动
- 次版本：向下兼容的功能新增
- 修订号：Bug 修复

### 2. 利用 Git 平台自带的 Release 功能

在 GitHub / GitLab / Gitee 的“Releases”页面，基于某个 Tag 创建发布，系统会自动打包源码压缩包（zip/tar.gz）。  
你只需在 Release 描述里填上**本次更新的清单**，这就形成了一份天然可追溯的版本存档记录。

**Release 描述模板（可直接用）：**

```markdown
## 变更内容
- [新增] 用户管理页面导出功能
- [修复] 登录状态超时不自动跳转问题

## 部署注意事项
- 需执行数据库迁移脚本：db/migration/V2__add_user_table.sql

## 关联文档
- 接口文档见：http://xxx/api-docs
```
这样一来，每个版本有什么改动、要做什么操作，全在一起，团队成员或后续接手的人一目了然。

---

## 四、项目文档与代码同步存档

文档也放在仓库里，和代码一起打 Tag。建议仓库目录结构：
```
项目根目录/
├── src/                # 源代码
├── docs/
│   ├── api/            # 接口文档（若是 Swagger 自动生成的可忽略）
│   ├── architecture.md # 总体架构说明
│   └── adr/            # 架构决策记录（重要设计决定的背景）
├── README.md
├── CHANGELOG.md        # 自动或手动维护的变更日志
└── commit_template.txt
```

**强制对齐规则：**
- 任何接口、配置、启动方式的改动，**必须在同一 Pull Request 中更新** `README` 或 `docs/` 下对应文件，否则代码审查不通过。
- 这由代码审查者（每次 PR 的另一位开发者）检查，无需专人。

---

## 五、自动化：让 CI 帮你生成存档（零人力）

在项目的 CI 流水线（如 GitHub Actions）中增加一个步骤：**每次推送 Tag 时，自动生成 Release 和 CHANGELOG**。

这里以 GitHub Actions + [release-drafter](https://github.com/release-drafter/release-drafter) 为例，你只需复制一份配置文件即可：
```yaml
# .github/release-drafter.yml
name-template: 'v$RESOLVED_VERSION'
tag-template: 'v$RESOLVED_VERSION'
categories:
  - title: '🚀 新功能'
    labels:
      - 'feat'
  - title: '🐛 Bug 修复'
    labels:
      - 'fix'
  - title: '📝 文档'
    labels:
      - 'docs'
change-template: '- $TITLE @$AUTHOR (#$NUMBER)'
version-resolver:
  major:
    labels:
      - 'major'
  minor:
    labels:
      - 'feat'
  patch:
    labels:
      - 'fix'
```
效果：每次合并 PR 时根据标签自动聚类，完成后再一键发布 Release，变更记录自动生成，完全不用人手写。

---

## 六、日常操作速查（可直接打印贴工位）

| 场景                    | 命令                                                  |
| ----------------------- | ----------------------------------------------------- |
| 开始新功能              | `git checkout -b feat/user-export`                    |
| 提交代码（弹出模板）    | `git commit`                                          |
| 推送到远程              | `git push origin feat/user-export`                    |
| 合并主线后打发布标签    | `git tag -a v1.3.0 -m "版本说明"`                     |
| 推送标签                | `git push origin v1.3.0`                              |
| 查看所有版本            | `git tag -l`                                          |
| 查看某个版本的文档/代码 | `git checkout v1.3.0`（或直接在 Web 端 Release 下载） |

---

## 七、开头一周速成计划

1. **第一天**：各仓库根目录放入 `commit_template.txt` 并全员配置好，讲一遍前缀含义。
2. **第二天**：确定版本号命名规则，在一个仓库中实际打一次 Tag，并创建 Release 页面给大家看。
3. **第三天**：强制规则落地——在代码评审清单中加上“文档是否同步更新”，第一次合并时认真执行。
4. **一周内**：选一个小项目接上自动生成 Release 的 CI，体验一次“推送 Tag 即自动存档”的快感。

这样，**用 Git 管理版本存档的操作就变成日常开发的自然动作，不需要专人维护，项目越久积累越有价值。**